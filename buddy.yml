- pipeline: "My Test Pipeline (code edited!)"
  trigger_mode: "ON_EVERY_PUSH"
  ref_name: "master"
  ref_type: "BRANCH"
  trigger_condition: "ALWAYS"
  actions:
  - action: "Execute: npm test"
    type: "BUILD"
    working_directory: "/buddy/buddy-node-example"
    docker_image_name: "library/node"
    docker_image_tag: "10"
    execute_commands:
    - "echo 'hello world'"
    - "npm install"
    - "npm test"
    setup_commands:
    - "npm install -g gulp grunt-cli"
    mount_filesystem_path: "/buddy/buddy-node-example"
    shell: "BASH"
    trigger_condition: "ALWAYS"
